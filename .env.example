# Copy to .env and fill in. .env is gitignored — never commit real credentials.
FKE_USER=your.email@example.com
FKE_PASS=your-password

# Optional: override the portal base URL (note: http, not https)
# FKE_BASE_URL=http://funkidsentertainers.bykayo.digital
